# Chronicle 数据格式规范

> **本文档是项目本地数据的唯一格式标准。**  
> 所有读写逻辑（Load Data 自动保存、Load Local Data、日总结生成与缓存）均须遵循此规范。  
> 格式变更时：先更新本文档，再改代码，并递增对应 `format_version` / `prompt_version`。

---

## 1. 总则

| 约定 | 说明 |
|------|------|
| 时区 | 日期划分使用**上海时区（UTC+8）**。`YYYY-MM-DD` 表示该自然日 00:00:00 ~ 23:59:59（上海时间）。 |
| 用户维度 | 所有按日数据以 `{user_id}` 为目录名，**与 ES 环境无关**（prod / test / baidu_* 不影响路径）。 |
| 文件命名 | 每日一个 JSON 文件，文件名为 `{YYYY-MM-DD}.json`。 |
| 编码 | UTF-8，`ensure_ascii=False`，缩进 2 空格。 |
| 覆盖策略 | 同一用户同一天重复写入时**覆盖**旧文件（records 与 summaries 均如此）。 |

### 目录结构

```
项目根/
├── id_list.json                 # 用户列表与画像（见 §2）
└── data/                        # DATA_DIR，可通过环境变量覆盖
    ├── records/                   # 每日对话记录（见 §3）
    │   └── {user_id}/
    │       └── {YYYY-MM-DD}.json
    └── summaries/                 # 每日 LLM 总结（见 §4）
        └── {user_id}/
            └── {YYYY-MM-DD}.json
```

环境变量：

| 变量 | 默认 | 用途 |
|------|------|------|
| `DATA_DIR` | `./data` | 数据根目录 |
| `RECORDS_DIR` | `{DATA_DIR}/records` | 对话记录 |
| `SUMMARY_DIR` | `{DATA_DIR}/summaries` | 日总结 |
| `ID_LIST_FILE` / `USER_PROFILE_FILE` | `id_list.json` | 用户列表 |

---

## 2. 用户列表 `id_list.json`

**路径：** 项目根目录 `id_list.json`  
**形态：** JSON 数组，每项为一个用户。

### 基础字段（必填）

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 用户名 / 英文名 |
| `id` | string | 用户 ID（与 ES `userId`、本地目录名一致） |

### 扩展字段（可选，供日总结 LLM 使用）

| 字段 | 类型 | 说明 |
|------|------|------|
| `display_name` | string | 显示名；缺省同 `name` |
| `aliases` | string[] | 别名列表 |
| `how_others_call` | string[] | 他人对该用户的称呼 |
| `role` | string | 角色描述 |
| `organization` | string | 组织 / 部门 |
| `related_people` | string[] | 关联人物 |
| `watch_keywords` | object | 关注关键词，含 `finance` / `personnel` / `company` 三个 string 数组 |
| `notes` | string | 备注 |

### 示例

```json
[
  {
    "name": "amanda",
    "id": "nnYX4pzcnVC"
  },
  {
    "name": "Chunhao",
    "id": "nnqn4gCd9TC",
    "display_name": "黄春皓",
    "aliases": ["春皓", "黄春皓", "Chunhao"],
    "how_others_call": ["春皓", "春皓哥", "黄老师"],
    "role": "技术负责人",
    "watch_keywords": {
      "finance": ["税", "工资", "报销"],
      "personnel": ["招聘", "离职", "绩效"],
      "company": ["排期", "上线", "项目"]
    }
  }
]
```

---

## 3. 每日对话记录 `records`

**路径：** `{RECORDS_DIR}/{user_id}/{YYYY-MM-DD}.json`  
**format_version：** `1`（当前）  
**写入时机：** UI **Load Data** 从 ES 拉取成功后，服务端按天自动写入。

### 3.1 顶层结构

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `format_version` | string | 是 | 固定 `"1"` |
| `user_id` | string | 是 | 用户 ID |
| `date` | string | 是 | `YYYY-MM-DD` |
| `saved_at` | string | 是 | 写入时间，ISO 8601（含时区，如 `2026-06-23T18:30:00+08:00`） |
| `item_count` | number | 是 | `items` 数组长度 |
| `items` | array | 是 | 当日对话条目，见 §3.2 |
| `source` | object | 否 | 数据来源元信息，见 §3.3 |

### 3.2 `items[]` 单条记录

| 字段 | 类型 | 说明 |
|------|------|------|
| `timestamp` | string | 显示时间，`YYYY-MM-DD HH:MM:SS`（按 ES 文档 `zone` 换算） |
| `time_ms` | number | 毫秒时间戳 |
| `speaker` | string | 说话人角色，见 §3.4 |
| `text` | string | 转写文本 |

### 3.3 `source` 对象（从 ES 拉取时写入）

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | string | 固定 `"elasticsearch"` |
| `environment` | string | 拉取时的 ES 环境 ID（`prod` / `test` / `baidu_release` / `baidu_dev`），**仅作记录** |
| `fetched_at` | string | 从 ES 拉取时间，ISO 8601 |

### 3.4 `speaker` 枚举

| 值 | 含义 | ES 来源 |
|----|------|---------|
| `user` | 主体用户 | V-tag = 0 |
| `others` | 其他人 | V-tag = 1 |
| `unknown` | 未知 | V-tag = 3 |
| `background` | 背景音 / 其他 | 其他 V-tag |
| `marker` | 时间分隔标记 | 系统生成，`text` 为 `--- Time: HH:MM:SS ---` |

### 3.5 完整示例

```json
{
  "format_version": "1",
  "user_id": "nnRWbEXHxyA",
  "date": "2026-06-23",
  "saved_at": "2026-06-23T18:30:00+08:00",
  "item_count": 3,
  "source": {
    "type": "elasticsearch",
    "environment": "test",
    "fetched_at": "2026-06-23T18:30:00+08:00"
  },
  "items": [
    {
      "timestamp": "2026-06-23 09:15:00",
      "time_ms": 1750641300000,
      "speaker": "user",
      "text": "你好"
    },
    {
      "timestamp": "2026-06-23 09:15:05",
      "time_ms": 1750641305000,
      "speaker": "others",
      "text": "你好，请说"
    },
    {
      "timestamp": "2026-06-23 09:15:05",
      "time_ms": 1750641305000,
      "speaker": "marker",
      "text": "--- Time: 09:15:05 ---"
    }
  ]
}
```

### 3.6 UI 传输格式（API 兼容）

前端时间轴 API 返回的简化形状（内存中使用，**本地文件不采用此形状**）：

```json
{
  "date": "2026-06-23",
  "id": "nnRWbEXHxyA",
  "items": [ ... ]
}
```

读取本地文件时由 `local_data.to_ui_format()` 自动转换。旧版无 `format_version` 的 `{ date, id, items }` 文件仍可读取并归一化。

---

## 4. 每日总结 `summaries`

**路径：** `{SUMMARY_DIR}/{user_id}/{YYYY-MM-DD}.json`  
**prompt_version：** `v2`（当前，由 `SUMMARY_PROMPT_VERSION` 控制）  
**写入时机：** UI 点击「生成总结」或 API `POST /api/summary/generate` 成功后写入。

### 4.1 文件顶层结构

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `user_id` | string | 是 | 用户 ID |
| `user_name` | string | 是 | 用户名（来自 id_list） |
| `date` | string | 是 | `YYYY-MM-DD` |
| `generated_at` | string | 是 | 生成时间，ISO 8601 |
| `model` | string | 是 | LLM 模型名 |
| `prompt_version` | string | 是 | 提示词版本，当前 `"v2"` |
| `user_profile_snapshot` | object | 是 | 生成时的用户画像快照 |
| `source` | object | 是 | 输入数据统计 |
| `summary` | object | 是 | LLM 结构化输出，见 §4.2 |

### 4.2 `user_profile_snapshot`

| 字段 | 类型 | 说明 |
|------|------|------|
| `display_name` | string | 显示名 |
| `aliases` | string[] | 别名 |
| `how_others_call` | string[] | 他人称呼 |

### 4.3 `source`（总结输入来源）

| 字段 | 类型 | 说明 |
|------|------|------|
| `item_count` | number | 当日对话条目数 |
| `char_count` | number | 送入 LLM 的 transcript 字符数 |
| `es_refreshed` | boolean | 是否从 ES 重新拉取了当日数据 |
| `truncated` | boolean | transcript 是否被截断（超 `LLM_MAX_INPUT_CHARS`） |

### 4.4 `summary` 对象（LLM 输出 schema v2）

| 字段 | 类型 | 说明 |
|------|------|------|
| `title` | string | 当日标题 |
| `overview` | string | 总览 |
| `events` | array | 事件列表 |
| `discussions` | array | 讨论列表 |
| `people` | object | 人物相关 |
| `company` | object | 公司 / 业务 |
| `finance` | object | 财务 |
| `personnel` | object | 人事 |
| `open_questions` | string[] | 待确认事项 |
| `keywords` | string[] | 关键词 |
| `raw_markdown` | string | 可选；JSON 解析失败时的原始 LLM 输出 |

#### `events[]`

```json
{ "time": "HH:MM", "what_happened": "string", "outcome": "string" }
```

#### `discussions[]`

```json
{
  "topic": "string",
  "summary": "string",
  "participants_mentioned": ["string"],
  "decisions_or_todos": ["string"]
}
```

#### `people`

```json
{
  "subject_user_activity": "string",
  "others_activity": "string",
  "mentioned": [
    { "name_or_alias": "string", "context": "string", "relation_guess": "string" }
  ]
}
```

#### `company`

```json
{
  "plans_and_arrangements": ["string"],
  "projects_or_business": ["string"],
  "risks_or_blockers": ["string"]
}
```

#### `finance`

```json
{
  "mentioned": false,
  "items": [
    {
      "category": "tax|salary|reimbursement|budget|payment|invoice|cost|other",
      "detail": "string",
      "amount_or_terms": "string"
    }
  ]
}
```

#### `personnel`

```json
{
  "mentioned": false,
  "items": [
    {
      "category": "hiring|resignation|role_change|performance|policy|team|other",
      "detail": "string"
    }
  ]
}
```

### 4.5 完整示例（节选）

```json
{
  "user_id": "nnRWbEXHxyA",
  "user_name": "zhiyuan",
  "date": "2026-06-23",
  "generated_at": "2026-06-23T15:57:52.140896+08:00",
  "model": "qwen3.6-35b-a3b",
  "prompt_version": "v2",
  "user_profile_snapshot": {
    "display_name": "zhiyuan",
    "aliases": ["zhiyuan"],
    "how_others_call": []
  },
  "source": {
    "item_count": 911,
    "char_count": 28907,
    "es_refreshed": true,
    "truncated": false
  },
  "summary": {
    "title": "2026-06-23 日常会议与业务沟通纪要",
    "overview": "主要讨论了 APP 技术故障排查……",
    "events": [
      {
        "time": "09:31",
        "what_happened": "讨论 APP 界面交互",
        "outcome": "确定优先解决功耗问题"
      }
    ],
    "discussions": [],
    "people": {
      "subject_user_activity": "",
      "others_activity": "",
      "mentioned": []
    },
    "company": {
      "plans_and_arrangements": [],
      "projects_or_business": [],
      "risks_or_blockers": []
    },
    "finance": { "mentioned": false, "items": [] },
    "personnel": { "mentioned": false, "items": [] },
    "open_questions": [],
    "keywords": []
  }
}
```

---

## 5. API 与读写流程

### 5.1 从 ES 拉取并保存 records

```
POST /api/data
Body: { "user_id", "start_date", "end_date", "environment" }
```

- 按上海时区逐日查询 ES
- 返回 UI 格式 `{ date, id, items }[]`
- **同时**按 §3 格式写入 `records/{user_id}/{date}.json`

### 5.2 从本地读取 records + summaries

```
GET /api/data/local?user_id={id}
GET /api/data/local?user_id={id}&date=2026-06-23
GET /api/data/local?user_id={id}&start_date=...&end_date=...
```

| 参数 | 说明 |
|------|------|
| `user_id` | 必填 |
| `date` | 可选，单日 |
| `start_date` / `end_date` | 可选，日期范围；均未传时默认为上海时区**当天** |
| `include_summaries` | 默认 `1`；`0` 则不附带总结 |

响应：

```json
{
  "success": true,
  "source": "local",
  "data": [ { "date", "id", "items" } ],
  "summaries": {
    "2026-06-23": {
      "success": true,
      "summary": { ... },
      "generated_at": "...",
      "prompt_version": "v2",
      "model": "..."
    }
  },
  "dates_found": ["2026-06-23"],
  "summary_dates": ["2026-06-23"]
}
```

UI **Load Local Data** 按钮：读取当天本地 records，并加载已有 summaries。

### 5.3 生成 / 读取总结

```
POST /api/summary/generate   # 生成或返回缓存
GET  /api/summary            # 读取单日缓存
GET  /api/summary/list       # 列出日期范围内已有总结
```

---

## 6. 版本与变更规范

### 6.1 当前版本

| 数据类型 | 版本字段 | 当前值 | 实现位置 |
|----------|----------|--------|----------|
| 对话记录 | `format_version` | `1` | `local_data.py` → `local_data.py` |
| 日总结 | `prompt_version` | `v2` | `summary_service.py` |
| 用户列表 | — | 无版本号 | `id_list.json` |

### 6.2 变更流程

1. **先改本文档**，说明新增 / 删除 / 变更的字段及原因。
2. **递增版本号**（records 改 `format_version`，summary 改 `prompt_version`）。
3. **改代码**：写入侧用新格式；读取侧在 `_coerce_record` 等函数中兼容旧版。
4. **不破坏路径规则**：始终 `{user_id}/{YYYY-MM-DD}.json`，不因环境或版本改变目录结构。

### 6.3 版本历史

| 版本 | 类型 | 说明 |
|------|------|------|
| `1` | records | 统一本地记录格式：`format_version`, `user_id`, `saved_at`, `item_count`, `items`, 可选 `source` |
| `v2` | summary | 结构化日总结 schema（title / overview / events / discussions / people / company / finance / personnel） |

---

## 7. 相关代码索引

| 模块 | 职责 |
|------|------|
| `local_data.py` | records 读写、格式归一化 |
| `save_to_json.py` | ES 查询与 `items` 提取 |
| `summary_service.py` | 总结生成与 summaries 缓存 |
| `app.py` | `/api/data`、`/api/data/local`、`/api/summary/*` |
| `static/app.js` | Load Data / Load Local Data 前端 |
| `prompts/summary_v2_schema.txt` | LLM 输出 schema 提示词 |
